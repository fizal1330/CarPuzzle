using UnityEngine;

public class FenceRotation : MonoBehaviour
{
    //private void Start()
    //{
    //    Debug.Log("FENCE ROTATION");

    //    // Get the fence's position in the grid
    //    int row = (int)transform.position.z;
    //    int col = (int)transform.position.x;

    //    // Check the position of the fence in the grid and rotate accordingly
    //    if (row == 0 && col == 0)
    //    {
    //        // Rotate the fence model for the top-left corner
    //        transform.localRotation = Quaternion.Euler(0f, 0f, 0f);
    //    }
    //    else if (row == 0 && col != 0)
    //    {
    //        // Rotate the fence model for the top-right corner
    //        transform.localRotation = Quaternion.Euler(0f, 90f, 0f);
    //    }
    //    else if (row == 3 && col == 0)
    //    {
    //        // Rotate the fence model for the bottom-left corner
    //        transform.localRotation = Quaternion.Euler(0f, 270f, 0f);
    //    }
    //    else if (row == 3 && col != 0)
    //    {
    //        // Rotate the fence model for the bottom-right corner
    //        transform.localRotation = Quaternion.Euler(0f, 90f, 0f);
    //    }
    //    //else if(col != 0)
    //    //{
    //    //    transform.rotation = Quaternion.Euler(0f, 90f, 0f);
    //    //}
    //}
}
